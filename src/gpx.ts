import type { Project, LatLng, Checkpoint } from './types';
export function gpxFor(project:Project,order:Checkpoint[],geometry:LatLng[]){const esc=(s:string)=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&apos;'}[c]!));const w=order.map((p,i)=>`<wpt lat="${p.geo.lat}" lon="${p.geo.lon}"><name>${esc(p.kind==='base'?'Baza':p.number)}</name><desc>Kolejność: ${i+1}</desc></wpt>`).join('');const tr=geometry.length?`<trk><name>${esc(project.name)}</name><trkseg>${geometry.map(p=>`<trkpt lat="${p.lat}" lon="${p.lon}"/>`).join('')}</trkseg></trk>`:'';return `<?xml version="1.0" encoding="UTF-8"?><gpx version="1.1" creator="Harpagan Route Planner" xmlns="http://www.topografix.com/GPX/1/1"><metadata><name>${esc(project.name)}</name><desc>Trasa Harpagana</desc></metadata>${w}${tr}</gpx>`;}
